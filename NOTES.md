# Board 架构与维护说明

写给以后要改这份代码的人，包括我自己、别的 AI、或者几年后已经忘记细节的你。
读完这一份就能安全地改动，不必逐行摸索。

---

## 一、这是什么

一个单文件的画布式笔记工具，用于文献管理与论文写作。核心主张是：
**信息之间的关联比信息本身更难重建**，所以位置、连线、分身、层级这些结构信息是一等公民，
导出时必须完整保留。

仓库里应当有这些文件：

```
index.html      程序本体，单文件，包含全部代码、样式、内置渲染库
manifest.json   PWA 清单，让它能装成桌面应用
icon-192.png    应用图标
icon-512.png    应用图标
tests.js        回归测试，改动后必跑
NOTES.md        本文件
```

`index.html` 可以脱离其余文件独立运行（图标以 data URI 内嵌了一份兜底）。
其余文件只影响"装成应用后任务栏显示什么图标"。

---

## 二、运行与测试

程序本身双击 `index.html` 即可运行，或部署到任意静态托管。

测试需要 Node 与 puppeteer：

```bash
npm i puppeteer          # 只需一次
node tests.js            # 跑全部
node tests.js text link  # 只跑名字里含 text 或 link 的组
HEAD=0 node tests.js     # 显示浏览器窗口，便于观察
CHROME=/path/to/chrome node tests.js   # 用已有的 Chrome，免下载
```

**任何改动之后都要跑一遍。** 这不是形式主义：历史上它抓到过箭头因 CSS 优先级而
完全隐形、导出图片一片空白、点工具栏导致文字选区丢失、函数自我调用造成死循环。
这些都是肉眼盯代码看不出来的。

---

## 三、数据模型

全部状态在一个全局对象 `S` 里。存盘就是把 `S` 序列化。

```js
S = {
  v: 3,              // 数据结构版本，见"版本迁移"
  cards: [...],      // 卡片
  links: [...],      // 连线
  frames: [...],     // 页面
  templates: [...],  // 本文件的卡片模板
  bg, lang, textDef, linkDef, lvStyle, readOrder, autoNum, work, ...  // 各类设置
}
```

### 卡片 card

```js
{
  id, x, y, w,        // 位置与宽度。高度不存，由内容撑开，测量后写入 h 供计算用
  h,                  // 测量得到的高度，off-screen 的卡片靠它参与连线定位
  text,               // 纯文本，检索与导出用
  rich,               // 可选，带局部格式的 HTML，存在时优先渲染
  s: {...},           // 卡片级样式：family size weight spacing lh align italic color
  bg,                 // 卡片底色
  ih,                 // 图片的内容哈希，图片本体不在这里
  ar,                 // 图片宽高比
  level: 1|2|3,       // 标题层级
  role: "apx"|"aside",// 附录 / 不参与编号
  tags: [...],        // 显式标签，正文里的 #标签 另行解析
  url,                // 来源链接
  ref,                // 分身：指向源卡片的 id
  sMan: true,         // 用户手动改过样式，分级统一时可跳过
  lock: true          // 锁定：不可移动、编辑、改样式、删除；但可做分身、随页面移动
                      // 无视觉标注，刻意为之：动不了本身就是提示
}
```

### 几条关键约定

**图片不在卡片里。** 图片按内容哈希存在 IndexedDB 的 `img:<hash>` 键下，卡片只存 `ih`。
这是性能的关键：画布数据只有几十 KB，自动保存瞬间完成，撤销历史也不复制图片。
导出 json 时才把图片内联回去（`bundle()`），导入时再拆开（`migrateInline()`）。
读图片一律用 `srcOf(card)`，不要直接读字段。

**分身是投影，不是复制。** `c.ref` 指向源，内容读源、位置样式各自独立。
读内容一律经过 `orig(c)`，这是最容易漏的地方：任何新增的"内容类"字段
（文字、图片、标签、链接）都必须走 `orig()`，否则分身会显示空白。

**锁定是卡片自身的属性，不是集合概念。** 早期版本用"编组"承载锁定，
维护成本高且与页面职责重叠，第 4 版取消，改为 `c.lock`。
迁移函数会把旧文件里"已锁定的组"的成员标记为卡片锁定，状态不丢。
连线没有独立的锁定标记，`isLinkLocked(l)` 由两端卡片推导。
批量锁定以页面为单位（`setFrameLock`），覆盖式写入，不做增量判断，
所以"单独解锁几张之后再整页锁一次"总能回到全锁状态，不必记录谁被解过。
曾经试过让锁定顺着连线扩散一层，逻辑绕且难预期，已废弃。

锁定只挡编辑不挡阅读：`.card.lk .cap` 保留 `user-select:text`，
锁定卡片的文字仍可划选复制。

注意 `setCardLock`（卡片锁定）与 `setLock`（演示模式）是两个不同的函数，
早期因重名导致前者被完全覆盖，测试里有一组守着这个。

**页面归属靠位置算，不存名单。** `frameOf(c)` 用卡片中心点判断落在哪个框里。
这样拖进拖出自动生效，永远不会出现"名单和实际位置对不上"。

**高度不是权威数据。** `hOf(c)` 优先取 `c.h`，其次取测量缓存，最后退回宽度。
`c.h` 由 `measure()` 在渲染后写入。曾经把 `hOf` 的定义写成自我调用，导致导入旧文件
直接死循环崩溃，测试里专门有一条守着这个。

---

## 四、渲染与性能

**只渲染视野内的卡片。** `syncCards()` 按视口加边距（`CULL.m`，默认 700）决定
哪些卡片进入 DOM，其余不创建节点。所以卡片总数不影响流畅度，五千张和五十张一样快。
`nodes` 这个 Map 保存 id 到 DOM 节点的映射，**不要用 querySelector 去找卡片节点**，
用 `nodes.get(id)`，因为视野外的根本不存在。

**连线也裁剪。** `drawLinks()` 跳过完全在视野外的连线，并且用 `drawLinksSoon()`
做每帧一次的节流。相机移动时不需要重画连线（它们在世界坐标里），这一点曾经写错，
导致缓动过程中每帧重建整个 SVG。

**样式改动不要整体重绘。** `setStyle()` 直接改已有节点的样式属性（`applyCardStyle`），
不调 `render()`。因为 `render()` 会销毁 contenteditable 元素，正在编辑的文字选区
随之消失，表现为"改字号没反应、数值跳回原值"。这是踩过的最深的坑之一。

**两处仍是全量扫描**：自动编号 `computeNums()` 和检索 `runFind()`。
几万张卡片时会有感知延迟，届时需要加索引或增量计算。目前规模无影响。

---

## 五、工具栏的两条铁律

浮动文字工具栏（`buildBar`）有两条不能违反的规则，违反了就会出现
"点了没反应"或"选区莫名消失"：

**一、工具栏任何位置都不能夺取焦点。** 在捕获阶段统一 `preventDefault()`
（滑块除外，否则拖不动），这样点分隔线、标签、空白处都不会清掉文字选区。
注意捕获阶段**不能** `stopPropagation()`，否则控件自身的处理函数收不到事件。

**二、字号框不是输入框。** 它是只读显示区，点击后由程序接管键盘
（`beginSizeEdit` / `sizeKey`）。因为只要输入框获得焦点，文字选区就必然失焦，
这是浏览器的固有行为，靠事后恢复永远不稳。

局部格式的写入一律走 `document.execCommand`，目的是进入浏览器的编辑历史，
让 Ctrl+Z 能逐步回退。自己直接改 DOM 会绕过历史栈。
需要包一层样式时用 `wrapSelWith()`，它会补回被 `cloneContents()` 丢掉的祖先元素
（浏览器不会克隆被部分选中的祖先，直接用会把已有的颜色和加粗一起抹掉）。

---

## 六、加一个新功能要碰哪些地方

这是本项目最大的维护成本来源。**加任何带数据的功能，以下七处逐一检查：**

1. **渲染** `makeCard()` 或对应的绘制函数。内容类字段记得走 `orig()`。
2. **菜单** `cardMenu` / `boardMenu` / `viewItems` / `fileItems`。
3. **文案** `T.en` 和 `T.zh` **同时**加，测试会检查两边一一对应。
4. **导入规整** `normCard()`。新字段要做类型校验，否则坏数据会让渲染崩溃。
5. **导出** 四条路径都要考虑：`exportDocx`、`docHTML`（PDF 与 HTML 共用）、
   `docMD`（Markdown 与存档包）、`captureCanvas`（画面）。
6. **版本迁移** 若改了数据结构，`SCHEMA` 加一，在 `migrate()` 里补一段。
7. **测试** 在 `tests.js` 里加一组，哪怕只有两三条断言。

漏掉第 4 条会导致旧文件打不开，漏掉第 5 条会导致资料无法归档，
这两样是最严重的。

---

## 七、版本迁移

`SCHEMA` 常量是当前数据结构版本，`migrate(d)` 是迁移链，按 `d.v` 逐级往上补。
原则：

- **只增不减。** 永远不要删除旧字段的读取逻辑，哪怕程序早就不用了。
- **容错优先于正确。** `normCard()` 遇到缺失或类型不对的字段一律修正为合理默认值，
  绝不抛错。导入失败比导入不完美严重得多。
- **比当前版本更新的文件也要尽力打开**，只在控制台留个警告。
  用户可能在新旧两台机器之间来回。

---

## 八、数据安全的四层

这是这个工具最要紧的部分，因为它承载的是多年积累。

| 层 | 防什么 | 机制 |
|---|---|---|
| 锁定 | 误碰已定稿内容 | 卡片级 `c.lock`，连带锁住相关连线 |
| 撤销 | 手滑 | 内存里 40 步 |
| 自动快照 | 误操作且已自动保存 | 每 10 分钟一份存进 IndexedDB，留最近 12 份加按天 7 份。因为图片单独存放，快照只有几百 KB |
| 绑定文件 | 浏览器数据被清 | File System Access API 自动写回本地文件 |
| 存档包 | 程序失效、平台变更、多年以后 | zip，内含分页 Markdown、图片原件、完整 json |

**存档包是最后的防线，它的价值在于不依赖本程序。**
`pages/` 是纯文本，`assets/` 是普通图片，用记事本和看图软件就能读，
也能直接倒进别的笔记软件。json 只是"能完美还原"的那一层，不是唯一出路。

zip 打包器（`zipMake`）是手写的六十行，无压缩、无依赖，离线可用。
不要为了省事换成外部库，那会重新引入网络依赖。

有两处保护是隐性的，改动时别弄丢：
清空画布会自动解除文件绑定；画布为空时暂停自动写回。
两者都是防止"清空后自动保存把原文件覆盖成空的"。

---

## 九、已知的取舍与限制

**图片有孤儿。** 替换或删除图片后，旧图仍留在 IndexedDB 里。没有做自动清理，
因为图片是跨画布共享的，自动清理有误删其他文件所需图片的风险。属于低频操作，
长期使用也不会明显累积。

**画面导出依赖网络字体。** 中文已全部改用系统字体，不受影响；
英文字体来自 Google Fonts，若未加载完就导出，可能截到回退字体。

**`buildBar` 偏长**（三百行上下）。它是目前最该拆的一处。
若要重构，注意上面第五节的两条铁律。

**何时该拆文件**：自有脚本超过 250 KB，或某个功能需要同时改七处以上时。
拆成 ES 模块在静态托管上没问题，代价是不能再双击本地文件直接打开
（模块受同源限制），需要加一个把模块拼回单文件的构建步骤。

---

## 十、改动这份代码的建议做法

不要用字符串批量替换。这份代码里出过两次事故都源于此：
一次把 `hOf` 的定义本身也替换了，变成自我调用；
一次把中文文案插进了英文文案区。

如果非要批量替换，**排除定义本身**，并且改完立刻跑 `node tests.js`。

改完之后的检查顺序：

1. `node tests.js` 全绿
2. 手动开一次程序，随便点点，看控制台有没有报错
3. 导出一次存档包，解压看看内容对不对
4. 用旧版本的 json 文件导入一次，确认还能打开
