# Board 架构与维护说明

写给以后要改这份代码的人，包括我自己、别的 AI、或者几年后已经忘记细节的你。
读完这一份就能安全地改动，不必逐行摸索。

当前规模：272 KB，5474 行，291 个函数，330 条双语文案，46 条说明书词条，
数据结构版本 SCHEMA = 8，测试 50 组 569 条断言（含 cutout.html 那一组）。

---

## 一、这是什么

一个单文件的画布式笔记工具，用于文献管理与论文写作。核心主张是：
**信息之间的关联比信息本身更难重建**，所以位置、连线、层级、分身这些结构信息是一等公民，
导出时必须完整保留。

仓库里应当有这些文件：

```
index.html      程序本体，单文件，包含全部代码、样式、内置渲染库
cutout.html     抠图工具，独立单文件，与 board 同一套视觉语言
manifest.json   PWA 清单，让它能装成桌面应用；shortcuts 里带一条抠图工具
icon-192.png    应用图标
icon-512.png    应用图标
tests.js        回归测试，改动后必跑
NOTES.md        本文件
```

`index.html` 可以脱离其余文件独立运行（图标以 data URI 内嵌了一份兜底）。
其余文件只影响"装成应用后任务栏显示什么图标"。

---

### 抠图工具 cutout.html

独立的单文件，不依赖 board，也不共享代码，只共用视觉语言（同一套字体、玻璃工具栏、浮层）。
做成两个文件而不是塞进 board，是因为它的交互模型完全不同：board 是画布上的多对象操作，
抠图是单张图上的路径编辑，混在一起两边的手势都会互相干扰。

**交互模型：卡点 + 沿边走。** 载入图片后直接进卡点模式，沿边缘随便点几下
（顺序不用管，`orderLoop` 用最近邻起手再拿 2-opt 解开交叉），按 Enter 走
`generateFromDraft`：两个卡点之间用 `livewire` 在梯度图上跑 Dijkstra 贴着边界连起来，
再 `chaikin` 平滑、`simplify` 抽稀、`makeSmooth` 求切线转成两侧带手柄的贝塞尔控点。
所以点得粗糙没关系（实测按 ±6% 抖动去点，生成的曲线仍然 100% 贴着主体），
真正决定贴合度的是沿边走，不是点得准不准。精修靠控点，尖角点与曲线点可以互换。
另有一套泛洪估背景的遮罩（`buildMask` / `largestBlob` / `traceContour`）供取样模式用。

**已知边界**：背景越干净越准；渐变背景靠容差滑块补偿；
完全贴边或与背景同色的主体得多点几个卡点手动圈。
没有做 GrabCut 或 ML 分割，因为要保持零依赖、可离线、单文件。

与 board 的衔接有两条，都不共享代码：抠完点「复制」，直接 Ctrl/Cmd + V 粘进 board
（board 那边的粘贴路径已经支持带透明通道的 PNG）；反过来，board 的画布右键菜单里
「抠图工具」一项（`openCutout`，紧挨着「插入图片」）在新标签里打开 cutout.html。
入口用 `new URL("cutout.html",location.href)` 解析，子目录部署和本地双击都找得到。
装成桌面应用的那条路也补齐了：`manifest.json` 的 `shortcuts` 里有一条指向 `./cutout.html`，
图标右键（Windows 任务栏 / macOS Dock / Android 长按）就能直接开抠图。
它落在 `scope: "./"` 之内，所以是在应用窗口里打开而不是丢给浏览器；
图标复用 `icon-192.png`，不必再多加文件。
**这只是一个入口，两个文件仍然各自独立**：cutout.html 不在旁边时，
无非是那个标签页打不开，index.html 本身照常运行，所以不做任何存在性检查
（file:// 上也检查不了）。`cutoutlink` 组守着这一条。

## 二、运行与测试

程序本身双击 `index.html` 即可运行，或部署到任意静态托管。

测试需要 Node 与 puppeteer：

```bash
npm i puppeteer                # 只需一次
node tests.js                  # 跑全部
node tests.js table outline    # 只跑名字里含 table 或 outline 的组
HEAD=0 node tests.js           # 显示浏览器窗口，便于观察
CHROME=/path/to/chrome node tests.js   # 用已有的 Chrome，免下载
```

**任何改动之后都要跑一遍。** 这不是形式主义。它抓到过的真问题包括：
箭头因 CSS 优先级而完全隐形、导出图片一片空白、点工具栏导致文字选区丢失、
函数自我调用造成死循环、卡片索引失效后读出 undefined、
抽取公共函数时漏改一处引用导致点击表格直接抛错、中英文案被插进同一个语言块。
这些都是肉眼盯代码看不出来的。

测试组一览：connectors 连线、text 文字格式、colors 颜色、twins 分身、
wrorder 写作页里的顺序、wrback 定位只留给引文分身、fmtbar 写作页工具栏、
wrver 版本复制到剪贴板、wrlevel 写作页里设定层级、wrexport 稿子导出、wrrefsel 引文可选中、wrcut 多选与剪切搬运、wrmarq 稿子里不许拉出画布的框选、wrwc 写作页字数统计、
frameown 页面归属、wrnum 用编号调位置、wrreford 引文长按调序、wrtitle 稿子标题点选、cutoutlink 抠图工具的入口、
pages 页面与层级、levelmark 层级标记、outline 结构连线、outdir 结构方向与批量转换、
lock 锁定、templates 模板、search 检索与链接、table 表格、tablemove 表格移动与删除、
tablesize 表格尺寸、cells 单元格选择、excel 与 Excel 互通、map 页面地图、
render 画面导出、archive 存档与分页导出、scale 整体缩放、data 数据安全、static 静态检查。

---

## 三、数据模型

全部状态在一个全局对象 `S` 里。存盘就是把 `S` 序列化。

```js
S = {
  v: 5,              // 数据结构版本，见"版本迁移"
  cards: [...],      // 卡片（表格是卡片的一种形态，不是独立实体）
  links: [...],      // 连线
  frames: [...],     // 页面（输出单元）
  sheets: [...],     // 标准尺寸页面（打印用），与 frames 完全独立
  wr: {ids,mode,twin},   // 线性写作：稿子的卡片顺序、整段/逐句、是否显示分身
  templates: [...],  // 本文件的卡片模板
  outline: true,     // 大纲是否跟随结构连线
  autoNum, readOrder, lvStyle, textDef, linkDef, linkView, linkLabel,
  bg, lang, work, bar, imgMax, findMode, findMerge,
  lastInk, lastHL, lastBg, lastTpl,   // 各种"上次用的"，一击即用靠它
  map, mapW, mapH,                    // 页面地图开关与面板尺寸
}
```

### 卡片 card

```js
{
  id, x, y, w,        // 位置与宽度。高度不存，由内容撑开
  h,                  // 测量得到的高度，视野外的卡片靠它参与连线定位
  text,               // 纯文本，检索与导出用
  rich,               // 可选，带局部格式的 HTML，存在时优先渲染
  s: {...},           // 卡片级样式：family size weight spacing lh align italic color
  bg,                 // 卡片底色
  ih, ar,             // 图片的内容哈希与宽高比，图片本体不在这里
  level: 1|2|3|...,   // 标题层级，不限于三级
  role: "apx"|"aside"|"bib",   // 附录 / 不参与编号 / 文献条目
  vers: [{id,name,text,rich,t}],  // 版本迭代，见"线性写作"
  wrIn,               // 只活在这份稿子里（见"写作页"）
  verOn,              // 当前启用的版本 id
  bib,                // 原文归属的文献条目 id（不画连线，纯数据关系）
  tags: [...],        // 显式标签，正文里的 #标签 另行解析
  url,                // 来源链接
  ref,                // 分身：指向源卡片的 id
  lock: "text"|"all", // 锁定级别，见下
  sMan: true,         // 用户手动改过样式，分级统一时可跳过
  tb: {...},          // 表格数据，见下
}
```

### 表格 c.tb

```js
{
  cols: [宽度...],          // 列宽，卡片宽度 = 列宽之和
  rows: [[单元格文本...]],  // 纯字符串，换行用 \n
  head: true,              // 标题行
  headCol: false,          // 标题列
  fmt: { "r,c": {al,b,i,bg,co} }   // 逐格格式，稀疏表，可以没有
}
```

### 连线 link

```js
{ id, a, b, kind, dash, arrow, w, color, note, sa, sb,
  st: true }        // 结构连线：表示"下级"，参与大纲与编号。没有这个标记就是关联连线
```

---

## 四、几条关键约定

**图片不在卡片里。** 图片按内容哈希存在 IndexedDB 的 `img:<hash>` 键下，卡片只存 `ih`。
这是性能的关键：画布数据只有几十 KB，自动保存瞬间完成，撤销历史也不复制图片。
导出 json 时才把图片内联回去（`bundle()`），导入时再拆开（`migrateInline()`）。
读图片一律用 `srcOf(card)`，不要直接读字段。

**分身是投影，不是复制。** `c.ref` 指向源，内容读源、位置样式各自独立。
读内容一律经过 `orig(c)`，这是最容易漏的地方：任何新增的"内容类"字段
（文字、图片、标签、链接、表格）都必须走 `orig()`，否则分身会显示空白。
**导出路径同样如此**：docx 与 html 的正文、摘要都要读 `orig(c)`，
否则分身在文档里会输出成空段落。
分身创建时默认写入 `lock:"text"`，因为在分身上误改就是改到原文。

分身在导出时**默认完整呈现内容**，不写成"另见 §x"。因为用分身构建文献引用时，
同一条引文在不同章节都应当读得到。导出面板有开关可切回交叉引用（`o.twinRef`）。

**锁定分两级**：`c.lock` 取 `"text"`（仅锁内容）或 `"all"`（内容与位置都锁）。
判定用两个函数，不要混用：`isLocked(id)` 管内容（编辑、格式、删除、层级、标签），
`isPinned(id)` 管几何（移动、缩放、对齐、连线起点、列宽拖动）。
对应地有 `freeSel()` 与 `movableSel()` 两套选集，改动时想清楚该用哪个。

拖动的语义随级别而变：`"text"` 时卡片可移动，所以正文 `user-select:none`，拖动即移动；
`"all"` 时卡片钉住，改为放开 `user-select` 并在 pointerdown 提前返回，把拖动让给文字选择。
放开时必须连 `.card` 这一层一起放开，只放开 `.cap` 无效，因为外层是 `user-select:none`。

**框选不排除锁定卡片。** 选中本身不改变任何东西，保护在每个操作里各自判断。
早期版本在框选阶段就把锁定卡片剔除，结果是锁上之后连"选中它们一起解锁"都做不到。

**标准尺寸页面（`S.sheets`）与页面（`S.frames`）是两套东西，不要合并。**
标准页只按毫米画出纸张区域供打印存档，不参与 `frameOf` / `pageGroups` / `docOrder` / 编号 /
分页导出。尺寸由 `PAPER` 表加 `MM`（96dpi）算出，不存宽高，改尺寸只换 `kind`。
打印走 `printSheets`，用 `captureCanvas(scale,list,rect)` 的第三个参数按纸张边界精确截取
（`pad` 置零，不加留白），并把 `@page size` 设成对应毫米数。
`worldBox()` 让"适应画面"把 frames 与 sheets 一并算进取景，否则只有它们没有卡片时取不到景。

**连接点要离开卡片本体。** 早期热区 30px 且居中压在卡片边缘上，
稍微靠边拖动就被它抢走，变成误连线。现在热区收到 18px、外移 16px，
且只有选中的卡片才亮出连接点，另外要求拖够 8px 才算真的想连线。
这三条缺一不可，改动时不要只留其中一条。

**页面归属靠位置算，不存名单。** `frameOf(c)` 用卡片中心点判断落在哪个框里，
配合 `frameIndex()` 的二分查找。拖进拖出自动生效，永远不会出现"名单和实际位置对不上"。
批量锁定与批量改连线身份都以页面为单位，覆盖式写入，不做增量判断。

**表格是卡片的一种形态。** `makeCard` 检测到 `c.tb` 时走 `buildTable`，
然后与普通卡片共用 `finishCard`（选中框、控制点、连接点、右键）。
这样位置、选中、移动、锁定、删除、页面归属、导出顺序、整体缩放、分身全部复用。
`c.text` 由 `syncTable` 维护为单元格文本的制表符拼接，检索和摘要因此自动可用。

表格的手势是两段式：未选中时拖动 = 移动整张表；选中后在格子上拖动 = 框选单元格。
不这样分，移动和框选会抢同一个动作。删除同理：有单元格选区时清内容，没有时删整张表。

列分隔把手按**真实渲染出来的列边界**定位（`alignGrips`），不能用列宽累加值，
因为折叠边框会让两者有零点几像素的出入，列一多就累积成肉眼可见的错位。

**高度不是权威数据。** `hOf(c)` 优先取 `c.h`，其次取测量缓存，最后退回宽度。
曾经把 `hOf` 的定义写成自我调用，导致导入旧文件直接死循环崩溃，测试里专门守着这个。

---

## 四点五、写作页

`S.docs = [{id,x,y,title,ids:[],mode:"iter"|"ctx",open:{}}]`。
稿子是**画布上的一页**，可以有很多份、各自命名，由 `renderDocs()` 画在 `#docs` 层，
正文就地可编辑，放大只是看得更清楚。全屏的"专注模式"是同一份内容加一栏目录，
两边共用 `docBodyHTML()` 生成正文、`bindBody()` 处理交互、`redrawDocs()` 一起刷新。

**送进稿子的是内容的独立拷贝，不是原卡片的另一份投影——这是整块写作页最核心的一条设计，
改动前务必先读这一段。** 早期版本（`wrImport` / `wrPasteMain` / `wrPasteAtCaret` 刚做出来时）
图省事，直接把原卡片的 id 塞进 `d.ids`，理由是"检索、导出、画布显示全都自动同步，
不需要任何双向同步逻辑"。这个想法本身没错，但代价是同一张卡片会**同时活在两个地方**：
画布上它自己的位置，和某份稿子的正文里。所有"这张卡片现在该用哪个 DOM 节点、
该不该弹工具栏"的判断都得去猜"用户现在到底在哪边操作"，而这个猜测天然不可靠——
连续出现过好几次真实的严重回归：标准工具栏在"外部卡片"（跟稿子无关的原始画布位置）
上完全弹不出来、工具栏定位跑去稿子里的另一份拷贝、两条工具栏叠在一起打架。
每次都是照着症状加一层新的判断（先是 `docsCapNodes.has(id)`，后来改成看
`document.activeElement` 在不在 `#docs` 里），越补越复杂，而且每一层判断本身都可能
有没覆盖到的场景，本质上是在给一个错误的数据模型打补丁。

真正的修法是把数据模型改对：`wrImport(ids,append,docId)` 现在通过 `wrCloneCard(c,d)`
只读原卡片的内容（文本、富文本、样式、底色、层级、表格、标签、版本……凡是
`cleanCard(orig(c))` 里有的都照抄），落地成一张**全新 id、带 `wrIn` 标记**的独立卡片，
原卡片在画布上的位置、内容完全不受影响。`wrPasteMain` / `wrPasteAtCaret`（右键粘贴到
正文顺序、粘贴到光标处）同理，通过 `wrClonesFromClip(d)` 直接复用 `CLIP.items[].snap`——
跟 `pasteClip()` 粘到画布空白处是同一份数据、同一个道理，不是另写一套。
这样一来，**一个 id 要么只活在画布上（在 `nodes` 里），要么只活在某份稿子里
（在 `docsCapNodes` / `wrmainCapNodes` 里），两边永远不会同时命中同一个 id**。
`hostOfCard` 因此可以放心地按"专注模式 → 画布内嵌的稿子 → 画布卡片本体"这个固定顺序
查找，不需要再看 `document.activeElement` 猜测当前上下文；`syncBar()` 判断要不要隐藏
浮动工具栏，也只需要看`sel` 里的 id 在不在 `docsCapNodes` 里就够了。见下面"工具栏"
那几段的具体说明——如果发现自己又在写"猜当前在哪个视图里操作"这种代码，
先停下来想想是不是又在往同一个坑里填，而不是先修数据模型。

**送进稿子的顺序由结构连线决定，空间位置其次，剪贴板的先后从来不作数。**
这是上面那条"送进稿子的是拷贝"的配套约定，一起读才完整。踩过的坑是这样的：
框选得到的 `sel` 是 `S.cards` 的数组顺序，也就是**卡片的创建先后**
（见 `startMarquee` 里的 `S.cards.filter(...)`）；`clipCards()` 原样存进 `CLIP.items`；
`wrClonesFromClip()` 又原样落地成稿子里的段落。整条路上没有任何一步看过结构连线，
于是用结构线挂在某个二级标题后面的正文，只要它建得比标题早（做笔记时这是常态：
先摘正文、后补标题），就会整批浮到稿子最前面，用户看到的就是"层级信息完全错乱"。

现在写作页所有的入口统一走 `wrOrderIds(ids)` / `wrOrderItems(CLIP.items)`
（`wrImport`、`wrClonesFromClip`、`wrPasteTwin` 三处，底层同一个 `wrOrderRank`）：
树里的按深度优先展开——标题在前、挂在它下面的正文紧跟其后；
树上没有的（或者画布上压根没有结构连线时的全部）再按 `flow()` 的空间阅读顺序接在后面；
源卡片已经删掉、排不进树的条目保持原有先后排在最后，绝不丢条目。

**这里用的是 `buildTree().roots` 的深度优先展开，不是 `.flat`——这一条不要"顺手简化"。**
`.flat` 还额外过了一道 `pullTogether`：关联连线（非结构线）会把相关的正文拉到相邻位置，
那是**导出成文**时的排版意图，用在写作页上就会把一段正文从它的结构父节点下面拽走。
实测复现过 `H1 → B21a → H11 → B11a…`，B21a 被拽到了第一章标题正下方。
导出路径继续用 `.flat`，两边各取所需，互不影响。`wrorder` 组同时守着这两条。

代价是粘贴时要建一次大纲树（跟编号是同一个 `buildTree`，量级也一样）：
三千张卡片粘四十张 33ms，两万张粘四十张 138ms，都在可接受范围；
只粘一张时提前返回，压根不建树。真正贵的是"全选整块画布一次性粘进稿子"
（两万张约 9 秒），那不是实际用法——一次送一章即可。若以后真要支持，
该做的是缓存 rank，而不是退回按剪贴板顺序排——但实际用法是一次几十张，
这条路大概永远不必走。

**分身/引用（挂在折叠框里的 `.ref`，`wrPasteTwin` 建的那种）是唯一的例外，
它们要的就是"跟着源卡片一起变"的活引用**，所以仍然用 `c.ref` 指回源卡片，
不走拷贝这条路。除了分身之外，写作页里能看到的任何段落——不管是从画布导入的、
剪贴板粘进来的、还是稿子里自己新建/切分出来的——现在统一都是独立内容，
编辑它们不会碰到画布上的任何卡片，删除它们也不会让画布上凭空少一张卡片。

**画布上的实体不要乘 `--iz`。** `--iz` 是 1/缩放，给的是"希望在屏幕上保持固定大小"的界面元素
（连接点、控制点、徽标）。稿子是世界坐标里的实体，内部一律用普通像素，随画布一起缩放。
早期给稿子的内边距和字号乘了 `--iz`，画布缩小时它们反而暴涨，把文字挤成竖排一列。

**稿子顶上有一条常驻的文字工具栏**（`buildFmtBar`，专注模式与画布内嵌视图共用）。
写作页长度固定，工具栏钉在最上方就不必为了改个颜色一路往上拉。
它调用的是画布那一套完全相同的函数（`cmd` / `setInkColor` / `setHighlight` /
`setAlign` / `setSize` / `clearFmt` / `setTextLink` / `setFamily` / `setStyle` /
`setCardBg` / `applyTemplate` / `setCardLock`），**不要另写一份实现**——早期写作页里那条
工具栏就是个只有样子的空壳，点了什么都不会发生。现在它是标准工具栏（`buildBar`）
常用操作的完整对照：字重字形与链接、字体、字号、文字色与荧光笔、对齐、字重与字距行距、
卡片底色、模板、清除格式、锁定，一样不少，只是字号仍然只用加减按钮（见下一段）。
第七节的两条铁律在这里同样适用：`preventDefault()` 与 `stopPropagation()` 分两条监听器挂，
不能合成一条捕获阶段监听——合成一条会连控件自己的处理函数都收不到事件；
新增的字距/行距滑块因此也要排除在 `preventDefault()` 之外，否则拖不动。
字号只用加减按钮改（输入框一拿到焦点，选区必丢），这条没有变。

**工具栏与样式在两种视图里共用一套**：`hostOfCard(id)` 返回卡片在当前视图中的宿主元素
（专注模式下是写作正文里的段落、其次是画布上内嵌的稿子段落，最后才是画布卡片本体），
`capOf` / `applyCardStyle` / `syncBar` 都走它。这样文字工具栏、局部格式、样式应用完全不必写第二份；
新增任何"根据卡片 id 找它在屏幕上的节点"的逻辑，都应该经这里，不要直接查 `nodes`。
`hostOfCard` 能写得这么简单（专注模式 → `docsCapNodes` → `nodes`，固定顺序，不用猜场合），
前提是上面那条"送进稿子的是拷贝"成立——两个 Map 和 `nodes` 之间没有 id 重叠，
不要在没有这个前提的情况下改回"原 id 直接进 `d.ids`"的老路。

**画布上稿子的左上角外侧只挂标题，不放功能控件。** `renderDocs()` 里 `.dttl` 曾经把
模式切换（迭代/Context）、专注、导出这几个按钮跟标题一起摆在页面外侧（`bottom:100%`），
这些按钮其实是"页面"本身的功能，摆在外侧会让页面的边界变得含糊。现在只有标题留在
`.dttl` 里，模式/专注/导出移进了页面内部的 `.dbar`，跟文字工具栏并排（靠右对齐），
按钮复用全局的 `.wb` 样式——这跟专注模式的 `#wrbar`（`fmtbar` + 模式按钮 + 导出 + 关闭
一起摆在页面内部）是同一种布局，两处不再各说各话。

**写作页里编辑段落时，浮动的标准工具栏（`#bar`）不再弹出。** `fmtbar` 补全成标准工具栏的
完整子集之后，两条栏同时出现纯属重复，还会叠在一起互相遮挡（`#bar` 贴着卡片浮动定位，
`fmtbar` 钉在页面顶部，选中的段落一靠上，两条就叠住了）。`syncBar()` 现在一开头就判断
`wrOpenState()`（专注模式）或 `sel` 里有 id 命中 `docsCapNodes`（画布内嵌的稿子），
命中就直接隐藏 `#bar` 并 `return`，不再往下走定位与内容同步那一段。
这个判断能这么直接，正是因为上面那条"送进稿子的是拷贝"：`docsCapNodes` 里的 id
不会跟画布上的普通卡片重叠，选中画布上完全没进过任何稿子的普通卡片时，
`#bar` 一直照常弹出；哪怕一张卡片被复制进过某份稿子，它在画布上的原始位置
也完全不受影响，因为稿子里那份根本是另一个独立的 id。

**字数统计：中日韩按"字"、拉丁按"词"，相加才是中英混排稿子里说的字数。**
只数字符或者只按空格分词，在这种稿子上都会算得离谱。数的是纯文本 `o.text`，
不碰 `rich`——格式标签不该算进字数。`wcDoc(d)` 只数这份稿子自己的段落：
折叠框里的引文是别人的话、其他版本是没被采用的稿子，都不算"我写了多少"。
划选优先：`wcSelection()` 用 `anchorNode` 反查宿主（`#wrmain` 或某个 `.doc`），
所以画布上同时摆着好几份稿子时，只有选区所在的那一份换成"选中 N"。
读数挂在 `selectionchange` 上，并到一帧里重算（`wcSoon`）；
打字那一路由 `refreshDocMeta()` 顺带刷新。它是**读数不是按钮**，摆在工具栏最后。

**`fmtbar` 按功能分成五组，组内不许被换行拆开。** 早先它是一长串按钮加分隔线，
每两三个就来一条竖线，顺序也没有道理（卡片底色夹在段落细项和模板中间，
跟另外两个颜色隔着半条栏），窄一点就折成两三行、还跟右边的模式/导出按钮挤在一起。
现在每一组装进一个 `.fgrp`（`flex:none`），换行只发生在组与组之间：
① 文字（B/I/U/S、链接）② 字体与字号 ③ 颜色（文字色、荧光笔、整段底色）
④ 段落（三个对齐 + 「间距」弹层：字重、字距、行距）⑤ 整段（模板、锁定）⑥ 清除格式。
分隔线只出现在组之间，`fmtbar` 组守着这条（组数、分隔线数、有无游离按钮、组是否被拆行）。

**弹层不许被稿子的右边缘切掉。** `.dwrap` 是 `overflow:hidden`（稿子要在固定高度里滚动），
所以靠右那几个控件（间距、锁定、清除那一带）的弹层向左对齐展开时会探出稿子外面，
被直接裁掉一截——滑块的数值就看不见了。`placePop(cell,pop)` 在每次打开时量一下：
探出去就改成右对齐（`left:auto;right:0`）。三条打开路径（色块、popCell、锁定）都要调，
`fmtbar` 组会把每一个弹层挨个打开，检查它是否完整落在 `.dwrap` 里。

**会变长短的标签用固定宽度，不是 `max-width`。** 字体名和模板名是跟着当前选择变的
（"IBM Plex Sans" → "苹方 / 微软雅黑"，"模板" → "想法和思考问题"），
用 `max-width` 的话每换一次，后面所有按钮都会跟着左右挪一下，
到了临界宽度还会整组跳到下一行——用户看到的就是"打开弹层挑一个，符号位置自己变了"。
`.lbl` 现在是 `width:84px` 定死的，栏上任何一个按钮的位置都不再跟名字长短有关。
固定的字（比如「间距」）不要挂 `.lbl`，否则会被撑出一大段空白。

**折行点是指定的，不是碰运气。** 画布上的稿子正文区固定 560px，工具栏必然要折行；
与其让它在临界宽度上飘忽不定，不如在颜色组之后放一个 `.fbrk`（`flex-basis:100%`）：
文字/字体/颜色一行，段落/整段/清除一行，两行长度接近，也正好是"改字"与"改段"的分界。
专注模式够宽，`#wrfmt .fbrk{display:none}` 把断点关掉，仍然是一行。

**引文投影长按可以调序，只在自己的引用框里挪。** 顺序就是 `wrKids()` 的顺序，
而 `wrKids()` 读的是 `S.cards` 的数组顺序——所以 `commitRefOrder(box)` 做的事是：
把这几张卡片在 `S.cards` 里**原位对调**（占用的还是原来那几个下标，别的卡片一张不动）。
不新增字段，存档格式不变，SCHEMA 不用加一。
为什么是长按（340ms）而不是按下就拖：这一块的文字现在是可以选中的（见下一条），
一按下就拖会把划字变得没法用；在长按计时走完之前只要移动超过 5px，就当成划字，调序取消。

**调序要做成"真的把它拿起来"，不要边拖边 `insertBefore`。** 第一版就是后者：
元素在脚下反复重排，落点判定又只在指针正好压住某一条时才生效，
快一点、偏一点就没反应，还会一路误触到别的内容。现在是：
长按到时间，这一条**离开文字流**（`.fb` 临时 `position:relative`，它改成框内绝对定位、
加阴影浮起来），原地插一个同高的虚线占位 `.refph`；拖动时只有占位在动，
松手把元素落进占位的位置。整个 `.fb` 加 `.reorder`（光标 grabbing、hover 反应关掉），
让"进入调序"这件事看得见。中途 `Esc` 就地取消，什么都不改。

**稿子内部的按下，一律不许冒到画布上去。** `.doc` 的 `pointerdown` 里原来对
`.ref/.fold/.vv/.wrnav` 这些控件是直接 `return`（不拦截），事件于是一路到达 `stage`，
在整张画布上拉起框选——表现就是"在稿子里按住一拖，蒙出一个灰色的大方框"。
那是画布圈选卡片用的 `#marq`，跟稿子里的文字选中完全是两回事，出现在这里毫无道理。
现在无论落在稿子的哪一处都先 `stopPropagation()`，再决定要不要拖动整页；
`closeMenus()` 单独补一句（原来是顺手由 stage 的 `pointerdown` 做的）。
专注模式不受影响：`body.wr` 下 `#stage` 是 `display:none`，本来就冒不过去。
`wrmarq` 组两头都守：稿子里拖不许出现 `#marq`，画布空白处必须照样框选得出来。

**划选与拿起必须分得开，这一条是这个交互能不能用的关键。** 同一块文字既要能划选
（引文就是拿来抄的）、又要能长按拿起来调序，两件事撞在一起的表现是：
按住想挪位置，屏幕上却一路刷出蓝色选区。分三层解决：
① 按住约 140ms 先加 `.armed`（浅底 + `cursor:grab`），让人知道"这一下正在被当作长按，
不是在划字"；一旦在长按计时走完前移动超过 5px，就撤掉预备态，把这一下让给划选。
② 拿起的那一刻给整个 `.fb` 加 `.reorder`，**连同子元素一起 `user-select:none`**——
只给被拖的那一条加没用，浏览器的划选是跨元素进行的。
③ 拖动过程中每一帧再清一次选区：CSS 的 `user-select` 只挡新的选区，
**挡不住鼠标按下时已经起头、还在延长的那一次**，光在拿起时清一次不够。
松手之后再清一次，免得落下来还留着一片选中的字。

两个细节，改动这一段时别退回去：
① **落点按指针算，不按浮起来那一条的位置算。** 后者被夹在框内（`maxTop`），
拖到最底下时它的中线可能差零点几像素够不到最后一条的中线，手感就是"怎么都放不到末尾"。
② **指针正好压在中线上时，按拖动方向决定归属**（往下拖归后一位、往上拖归前一位，
那半个像素的 `eps`）。不加这一条，"正好拖到最后一条中间"会被判成留在原处。
③ 画布上的稿子整体带缩放，屏幕像素要除以 `scale` 才对得上 `offsetTop`。

**稿子标题跟页面框的标题一个规矩：点选、拖动、右键菜单、双击改名。**
早先 `.dt` 是 `contenteditable`，点一下光标直接进去，想"选中这份稿子再按 Z 定位"
反而没有办法。现在点一下 `selectDoc()` 并可拖动整页，右键落到 `docMenu`（改名在里面），
双击才 `prompt` 改名。专注模式的 `#wrtitle` 仍然是就地编辑——那里本来就在写字，
不需要"选中"这个概念。

**M 开关页面地图那一句，原来被错缩进写在 `if(lock){...}` 里面。**
于是只有演示模式按得动，正常编辑时怎么按都没反应，而菜单上明明写着 `M`。
`map` 组现在按真键来验证。缩进错位造成的作用域错误不会有任何语法报错，
改这一段 `keydown` 时留意。

**不可编辑 ≠ 不可选中。** 引文投影是 `contenteditable="false"`，改不动它这一点已经够了；
`.ref` 上原来还挂着 `user-select:none`，于是连一句话都抄不出来——而引文的用处
恰恰就是抄一句放进正文。那条 CSS 已经删掉，`cursor` 也改成 `text`。
`wrrefsel` 组用真鼠标划一遍来守这条，同时确认它仍然改不动。

**稿子折叠框里的分身没有"位置"，跳转不能用 `focusOn`。** 它的 `x/y` 是拷贝时随手写的
稿子左上角，所以 `gotoTwin()` 轮到这种分身时，镜头只会停在稿子的左上角，
根本看不到那一条引文。现在改走 `revealRef(tw)`：把承载它的稿子打开、
展开它所在的折叠框、`scrollBlockInto` 滚到那一条，再闪一下（`.ref.flash`）好认。

**写作页里的"剪切"是搬运，不是拷贝。** 调段落顺序用的是 `WRCUT={doc,ids}` +
`wrCutPaste(d,i)`：只在 `d.ids` 里重新排位，**不新建任何卡片**，
所以层级、版本、底色、挂在段落下面的引文投影全都原样跟着走。
如果走剪贴板（`clipCards`/`wrPasteMain`），粘出来的是一份新的独立拷贝，
版本与投影会留在原地一起丢掉——两条路不要混。
剪切当下也不改数据，真正的搬运发生在粘贴那一刻，中途反悔什么都没损失。
插入点要换算成"移走之后"的位置，否则同一份稿子里往后挪会差几位；
跨稿子搬运时，段落和它下面的投影都要改 `wrIn`。

**写作页的多选：Shift 连选、Ctrl/⌘ 单个增减，都要 `preventDefault()`。**
段落是 `contenteditable`，不拦住的话浏览器会把 Shift+点击当成"扩展文字选区"，
光标一落进去，`focusin` 立刻把 `sel` 打回一段，多选永远选不起来。
右键同理：右键会让 `contenteditable` 拿到焦点，所以 `bindBody` 里用 `ctxKeep`
记一笔"这次右键点在多选里"，让那一次 `focusin` 放行——
不然菜单永远只认得"这一段"，多选剪切根本用不了。

**`frameOf()` 有两条铁律，都是踩过事故才写下来的。**
① **只活在稿子里的段落（`wrIn`）永远不属于任何页面。** 它们的 `x/y` 是拷贝时随手写的
稿子左上角，画布上根本不画它们；一旦被算成"页面里的卡片"，
`delFrame(f,true)`（删页面连同卡片）就会把稿子的段落一起删掉——
**用户看到的是"删了一页，旁边写作页的目录层级全没了"，而且完全联系不到原因。**
② **判定点不能依赖实测高度。** `hOf()` 在没量过时退回拿宽度当高度（可能差几百像素），
而只有渲染到屏幕上的卡片才量得到（有剔除）——于是同一张卡片"在视野里"和"在视野外"
会归属不同页面，表现就是移动页面时莫名其妙丢下几张、而且复现不了。
现在锚点是"卡片顶部往下 40px 以内"（`c.y+Math.min(hOf(c)/2,40)`），量没量过都是同一个答案。
副作用：高过 80px 的卡片按"顶部在哪一页"归属，而不是"中心在哪一页"——更符合直觉，
也更稳定。`frameown` 组守着这两条。

**导出的标题必须是真的 `h1`-`h6` 标签。** Word 打开 `.doc`、PDF 生成书签，
认的都是标签，不是"字大一点的段落"。用 `div` 的话，导出的文稿在 Word 里没有任何
大纲结构：导航窗格是空的，也生成不了目录——看起来像"有层级"，其实只是文本加数字。
字号字体仍然按卡片自己的样式内联覆盖，所见即所得不受影响。
`.docx` 那条路本来就用 `HeadingLevel`，两条路现在一致。

**用编号调位置（`wrMoveToNumber`）：点标题左边的编号、输入新编号，整节搬过去。**
三条规矩：① 搬的是**整节**（标题 + 它的正文 + 所有更低级的子标题），
只搬标题会把正文丢在原地；② 目标编号的**位数决定层级**（"2.1" 是二级），
所以把 3 移到 2.1 会顺带降级，子标题跟着同样的位移；
③ 编号是 `wrNums()` 现算的、不是存下来的，所以这里不能"改编号"，
只能反过来算"要得到这个编号该插在哪一位"——做法是把这一节抽出来，
在剩下的段落上逐位试插，看哪一位算出来正好是目标编号。
取**最后一个**匹配位而不是第一个："把第三章移到 2"，第一个匹配位是
"紧跟在第一章标题后面"（第一章的正文还没开始），那会把它插进第一章肚子里。

**导出 Word 必须永远有结果。** 真正的 `.docx` 要靠 CDN 上的 docx 库（`loadDocx()`），
失败的方式不止"网络连不上"一种：CDN 半截返回、拿到的 `window.docx` 缺东少西、
`Packer.toBlob` 自己抛错——**每一种都要落到 .doc**，所以 `wrExportWord()` 里
连"拿到库之后的构建"也一起包在 try 里，并且先检查 `Document/Packer/Paragraph/HeadingLevel`
都在才敢走 `.docx`。只包住 `loadDocx()` 是不够的，那样后面炸了照样弹"导出未完成"。
现在跟画布那边一个规矩：库拿不到（或者用不了）就退回写一份 `.doc`
（HTML 外壳 + `application/msword`），Word 一样打得开，样式、编号、底色全都在。
这一条不要"优化"成只留 `.docx`：这个程序的前提是断网也能用。

**标题编号由 `wrNumMap()` 统一提供，三种格式共用。** 稿子里看到的编号是 `wrNums()`
算的（侧栏目录与正文那一份），导出时必须是同一套规则，否则会出现"稿子里是 2.3、
Word 里却是 2.1"这种对不上的事。导出面板里有「标题编号」开关，默认勾上；
关掉就一个编号都不加。编号只加在标题上，正文段落不动。
`.docx` 那条路把编号交给 `paraFrom(c,D,extra,pre)` 的第四个参数当前缀 run 插进第一段，
不去碰原有的富文本（加粗、颜色、荧光笔都保住）。

**`safeName(x,i)` 的第二个参数是给存档包分页用的（`01-引言.jpg`），单文件导出不要传。**
早先写作页导出无条件拼 `String(i)`，于是每一个导出的文件都叫 `undefined-稿子.docx`。
现在 `i` 省略时只用标题本身。

**层级在写作页里也能设定，用的还是画布那一套 `setLevel()`。** 稿子里的标题层级就是
卡片的 `c.level`——侧栏目录、正文编号、导出的标题级别全都读它，没有第二份数据
（见第四节）。写作页原来只显示不设定，想把一段改成二级标题得先回画布上点右键菜单。
现在段落组里有「层级」四档（正文 / 标题 1-3），标签显示当前段落的层级；
底层直接调 `setLevel()`，不另起炉灶，所以层级附带的字号字重也一并套上。
一个坑：`setLevel()` 原来只 `render()`（管画布，含 `renderDocs`），
专注模式的 `#wrmain` 是另一棵树，不 `drawWrite()` 就不会跟着变，
所以那里补了一句。凡是能从写作页触发的画布函数，都要想一下这件事。

**弹层的宽度要按里面装的东西定。** 滑块有自己的固有宽度（约 129px），加上标签与数值
就是 195px，塞进默认的 150px 弹层里会直接从圆角框外面呲出来。「间距」那个弹层
单独给 `.fpop.para{width:212px}`，滑块与字重按钮都加 `min-width:0` 允许被压缩。
`fmtbar` 组会遍历弹层里的每一个元素，检查它们都落在弹层的边界内。

**写作页的锁定只有一档，是个开关图标，不挂弹层。** 稿子里的段落没有"位置"可锁——
它们的 `x/y` 只是拷贝时随手写的稿子左上角，画布上根本不出现。所以这里就是
点一下锁内容、再点一下解开；「锁定内容与位置」那一档留给画布上的标准工具栏 `#bar`。

**弹层永远不许是空的。** `popCell` 在 `build(pop)` 之后检查一遍：一个孩子都没有就补一句
`t("fmtNeedSel")`。没选中段落时，「间距」（`barCard()` 为 null）与「锁定」（`sel` 为空）
原本会弹出两个什么都没有的白盒子，看起来像坏了。这两个控件另外挂 `data-need`，
`syncFmtBars` 据此把它们淡下去（`.fx.off`），但仍然可点——点开看到的是提示。
字号在没选中时显示一个短横而不是空白，同理。

**光秃秃的小三角不要再出现。** 「间距」与「锁定」原来各是一个只有 `▾` 的按钮，
既看不出是干什么的，也没法通过图标认出来。现在「间距」带文字标签，
「锁定」做成跟色块一样的两半（点锁图标切换仅锁内容，小三角才展开四种操作）。

**清除格式的图标是橡皮擦，不是 ✘。** ✘ 在一条全是排版功能的栏里太像"删除"了。
擦掉的是格式不是内容，图标就该这么说。

**画布上的稿子，工具栏分两行：上面一行是文档级的（迭代/Context、专注、导出），
下面一整行让给文字工具栏。** 稿子正文区只有 560px 宽，两者挤在同一行时
文字工具栏被压成三行还跟按钮打架。分成两行之后宽度够用，功能层级也跟着清楚了
（`.dbar` 加 `flex-wrap`，`.dtool` 用 `order:-1` + `width:100%` 提到上面一行）。
专注模式的 `#wrbar` 够宽，仍然是一行。

**`fmtbar` 的色块（文字色/荧光笔/卡片底色）分成两半，点色块本身要直接生效。**
早期整块只会展开换色的弹层——划完选区想直接用当前色，还得先点开弹层再点一次色块，
多切换一步。现在 `swatchCell` 拆成 `.fx.half`（色块本身，点了直接 `setInkColor(lastInk())`
这类"套用当前色"）和 `.fx.caret`（小三角，点了才展开弹层换一个新颜色），
跟标准工具栏 `#bar` 里 `.cell.split` 的手感一致。

**折叠框不再挂 `title` 提示。** 早期在 `.fold` 上写了 `title="在这里粘贴分身"`，
悬浮久一点就弹出浏览器原生的提示气泡，界面上一直有个不请自来的东西，删掉了。
折叠框本身仍然是"看不见但确实存在的粘贴区"，右键照样能粘，只是不再用 `title` 提示这件事。

**新建版本、粘贴、插入这些带重绘的操作，不该把用户弹回稿子最前面。**
`renderDocs()` 和 `drawWrite()` 都是整段 `innerHTML` 重建，`.dbody` / `#wrmain`
作为全新的 DOM 节点，`scrollTop` 天然会被冲回 0——稿子一长，随手点一次"新建版本"
就跳回顶部，等于把人所在的位置弄丢。现在两处重建前都先记下当前的 `scrollTop`
（`renderDocs` 按 doc id 存进一个 `Map`，因为一次可能重建多份稿子；`drawWrite`
只有一份 `#wrmain`，存局部变量即可），重建完再照原样填回去。

**写作页就是一份可以直接打字的稿子。** `.cap` 默认 `contenteditable="true"`，
点哪里光标就落在哪里，不再有"先双击进入编辑"这一步——那一步是早期设计，
用起来像在填表格而不是在写东西。锁定的段落仍然只读。
配套的三个键盘行为都在 `bindBody` 的 `keydown` 里：
回车走 `wrSplitAtCaret()`，从光标处把这一段切成两张卡片（Shift+回车仍是段内换行）；
段首退格走 `wrMergeBack()`，把这一段并回上一段；
选中但未编辑时的退格仍走 `wrRemoveSel()`，只把它移出稿子。
划选一段自由文字后右键选"变成卡片"走 `wrSelToCard()`，
选区前后剩下的部分各自成段，顺序不变。
切分与合并都经 `cutAfterCaret()` / `caretAtStart()` 这两个基于 Range 的小工具，
不要改成按字符串位置算，富文本里的偏移量跟纯文本对不上。
这几个函数本来就是"从稿子内部现有的段落生成新的 wrIn 段落"，
跟这次改的"从画布/剪贴板导入时要不要拷贝"是两回事，不受这次改动影响。

**"粘贴可编辑卡片"要有一个具体的插入点，不能只有段前/段后两个位置。**
`wrPasteMain(d,i)` 原本只能把剪贴板里的内容整段插到某一段的前面或后面，
光标停在文字中间时没有对应的粘贴入口，容易被当成只有"粘贴分身引用"（`wrPasteTwin`，
挂在折叠框里）这一条路。现在 `wrBlockMenu` 会判断右键时光标是否正停在这一段文字中间
（`caretHere`，逻辑跟 `wrSelToCard` 判断有没有划选是同一处 `getSelection()`），
是的话菜单里多一项"粘贴到这里"，调用 `wrPasteAtCaret(d,c,cap)`：
复用 `wrSplitAtCaret` 同一套 `cutAfterCaret()` 把光标之后的内容切出来，
剪贴板里的内容拷贝（`wrClonesFromClip`）插进切开的缝隙，光标之后的内容再另起一段接在后面——
效果上就是"在光标这个具体位置粘贴"，而不是只能选段前或段后。

**稿子里的增删一律走右键，没有专属的小按钮。** 早期版本在段落之间放过一条悬浮的
`.wrins` 插入线，折叠框上放过 `.fadd`，版本栏上放过 `.vadd`——三个只在写作页出现的
点击目标，跟画布上"右键出菜单"的手势各说各话。现在全部删掉，统一由 `wrContextMenu()`
按落点分派：引用条目给 `wrRefMenu`，折叠区给 `wrFoldMenu`，段落给 `wrBlockMenu`，
空白给 `docMenu`。折叠区本身就是"看不见但确实存在的粘贴框"：它有 `min-height`，
即使没挂任何东西也能在那里右键粘贴分身。

**写作页里的任何删改都不影响画布上的任何卡片。** 这是这一块的基本原则，不是某个函数的
实现细节，也是"送进稿子的是拷贝"这条设计的直接推论。移出统一走 `wrDropAt()`：
它把 id 从 `d.ids` 里摘掉，**现在 `d.ids` 里的段落全部带 `wrIn` 标记**（不管是从画布
导入/剪贴板粘贴时拷贝进来的，还是稿子内部新建/切分出来的），本来就没有稿子之外的身份，
所以移出的同时会直接从 `S.cards` 里删掉，留着只会变成永远找不到的孤儿；
如果这段内容当初是从某张画布卡片拷贝来的，那张源卡片是完全独立的另一个对象，
不会被这里牵连。

**正文段落没有"在画布上找到它"，这是刻意的，不要加回来。**
稿子里的段落是内容的独立拷贝，画布上根本没有对应的实体：它自己的 `x/y` 是拷贝时
随手写的稿子左上角，高度也从没量过（`hOf` 在没有实测高度时退回拿宽度当高度），
拿它去 `focusOn` 取景，镜头就落在稿子旁边一块空地上——用户报的"定位有点偏"正是这么来的。

修过一版：给拷贝记一个 `c.wrFrom` 指回源卡片，定位时顺着它往上找。技术上完全跑得通
（实测镜头偏差 0,0），但那等于给"两边彻底独立"这条设计开了一个后门：
源卡片改名、移动、删除之后，这条线索或对或错，用户却分不清；
而这一段内容本来就跟那张卡片再无关系了——写作页里的删改从不影响画布，反过来也一样。
所以整套回滚，菜单项直接不提供，`c.wrFrom` 也没有留在数据结构里。
以后如果又想给正文段落加"回到原卡片"，先回来读这一段。

**但折叠框里的引文分身必须留着"跳转到原文位置"**（`wrRefGoto`，在 `wrRefMenu` 里）：
它是活的投影（`c.ref` 指回源），定位到原文正是它存在的意义。
两件事长得像，删的时候不要连坐。`wrback` 组同时守着这两头：
正文菜单里不许出现定位项、文案里也不许再有 `wrGoCanvas`，
而引文分身的菜单里必须有那一项，跳过去之后**按屏幕位置**量，镜头要正对原文
（世界坐标对不对不算数，看得见才算数）。

**`c.wrIn` = 这张卡片只活在某一份稿子里。** 判定用 `docOnly(c)`。
现在**稿子里能看到的段落全部带这个标记**：不管是 `wrImport` / `wrPasteMain` /
`wrPasteAtCaret` 从画布或剪贴板拷贝进来的，还是稿子里自己新插入的段落
（`wrInsertAt` / `wrSplitAtCaret` / `wrSelToCard`）和折叠框里的分身投影（`wrPasteTwin`），
统统如此。`syncCards()` 与 `docOrder()` 都跳过带 `wrIn` 标记的卡片，所以它们不上画布、
不参与编号与导出顺序。不这样做的话，在写作页里打一行字，画布上就会同时冒出一个文字框，
分身也会在稿子旁边多出一张卡片——用户看到的就是"同一份内容显示了两次"。
早期版本试过把它们摆到稿子右边的空地上（`d.x+DOCW+SIDEW+60`）来躲开重叠，
那只是让重复的东西离得远一点，没有解决重复本身；再往前，`wrImport`/`wrPasteMain`
还试过直接复用原卡片 id（不拷贝），代价是同一个 id 同时活在画布和稿子里，
详见本节开头那一大段——这条路走不通，不要再改回去。
`migrate()` 的 7 → 8 会给旧数据里已有的折叠分身补上这个标记。

**折叠的引用与分身是投影，不是内容。** 它们存在的意义是"定位原文和位置"，
所以 `.ref` 一律 `contenteditable="false"`，正文里也不写出条数与说明文字，
只留一个 `›` 折叠符号。右键给两项：`wrRefGoto` 跳到原文在画布上的位置，
`wrRefDrop` 撤掉这条投影——后者只删投影本身，源卡片纹丝不动。

**结构线的父子关系不看画线方向。** `structPair()` 的判据依次是：
先看 `lvRank`（标了 `level` 的一端永远是上级），
再看**结构线的度数**（谁被更多结构线连着，谁就是这一处的主心骨，
思维导图里中心节点连线最多），再看位置（上面/左边的作上级），
最后才回退到连线本身的方向。
中间那条度数判据是补上去的：早先两端层级相同时直接按连线方向定父子，
于是"标题没标 level、线又是从正文画向标题"这种极常见的画法会把整份大纲翻过来
（实测得到 `P1(lv1), H(lv0), P2(lv0)`，正文成了上级）。
`outline` 组守着这一条。

**不要用 `scrollIntoView`。** 它会连带滚动**所有**祖先滚动容器，
而 `#stage` 虽然写了 `overflow:hidden`，脚本照样滚得动
（`overflow:hidden` 只挡用户滚动，不挡 `scrollTop` 赋值）。
实测点一次大纲条目就把 `#stage` 滚了 236px：整张画布连同稿子一起错位，
可相机参数一点没变，排查起来很像"布局莫名其妙跳了一下"。
稿子内部的定位一律走 `scrollBlockInto(box, el)`，它只改目标容器的 `scrollTop`。
注意画布上的稿子整体带着缩放，屏幕像素要除以缩放比才能和 `scrollTop` 对齐。
另外 `#stage` 上挂了一个 `scroll` 兜底监听，谁把它滚起来就立刻归零：
画布的位移只能由相机负责。

**取景之前必须先把目标量准。** `hOf()` 在没有实测高度时会退回拿 `c.w` 当高度，
那是个纯粹的猜测；而 `syncCards()` 只渲染视野内的卡片，
所以聚焦一张视野外的卡片时它从来没被量过。
一张 340 宽、实际只有 23 高的卡片会被当成 340 高，取景框往下多出一大截，
表现就是"点了卡片 A，聚焦的却是 A 下方的空间"。
`focusOn()` 因此在 `bbox()` 之前先调 `ensureMeasured()`：
把还没量过的目标临时放进 DOM 量一次，量完交给下一轮剔除自然收走。
目标超过 `MEASURE_CAP`（400）就不逐个补量了，那时误差已被整体尺寸摊薄。

**聚焦要真的放大。** `focusOn()` 的 maxZ 早先写死成 1，
选中一张小卡片按 Z 只会把它挪到屏幕中心却完全不放大，用起来像没反应。
现在跟"适应画面"用同一个上限 1.6。

**稿子不是卡片，进不了 `sel`。** 所以另有一个 `selDoc` 记当前选中的稿子，
`selectDoc()` 负责让 `sel` / `selLink` / `selDoc` 三者互斥。
没有它的话"选中写作页再按 Z"永远不会有反应——因为那个状态压根不存在。
`focusSel()` 会先看 `selDoc`；选中的若是只活在稿子里的段落（带 `wrIn`），
则聚焦到承载它的那份稿子，因为那种段落在画布上没有独立位置。

**版本栏必须竖排。** `.gut` 是 `position:absolute; right:100%`，
早期横排加 `justify-content:flex-end`，每多一版就往左长一截，
版本一多，最早的几个会溢出到抓不到的地方，点都点不中。
现在 `flex-direction:column`、宽度恒定 40px，多少版都停在原地。
宽度是按最宽的罗马数字（VIII）量出来的：34px 时它会往左溢出 5px，实测抓不到。
`tests.js` 的 write 组守着这一条（连加七版之后所有 `.vv` 的 left 必须一致）。

**非专注状态下也常显大纲侧栏，正文本身固定高度、内部滚动**：`renderDocs()` 给每份画布内嵌的稿子
都加一个 `.dside`（用 `wrOutlineHTML()`，和专注模式的 `#wrside` 共用同一份生成逻辑），
`.doc` 总宽度是 `DOCW+SIDEW`，正文区 `.dbody` 固定高度、`overflow:auto`，
不再靠内容把整张卡片撑得没边——效果类似 Word 文档里固定视口 + 内部滚动条，
而不是打开专注模式才有大纲、平时却是一张无限拉长的卡片。

**四个踩过的坑，改这块时留意：**

1. **`hostOfCard` 不能现查 DOM。** 早期实现直接 `document.querySelector` 按 id 现查，
   `syncBar` 里 `sel.map(id=>hostOfCard(id))` 一旦选中成百上千张卡片（十万级画布的常态）
   就是等量的现查，会整屏卡顿、缩放平移都跟着掉帧。现在跟画布的 `nodes` 是同一个思路：
   `docsCapNodes` / `wrmainCapNodes` 两个 Map，只在 `renderDocs()` / `drawWrite()`
   批量建一次，之后 O(1) 查。以后任何"按 id 找它在写作页里的节点"都不要现查，走这两个 Map。
2. **选中状态要烧进 className，不能只靠事后 `paintSel()` 补一层。** `.blk` 的 `sel` 类
   现在跟画布卡片一样，在 `docBodyHTML()` 生成时就按 `sel`/`editing` 直接写进去。
   这是因为任何一次重绘（哪怕是别处打字触发的防抖刷新）都会整段重建 DOM，
   若选中状态只活在事后补上的 class 里，重绘一发生就悄悄丢了——
   而 Backspace 依赖 `.blk.sel` 判断"要不要只移出稿子"，一旦找不到就会落到画布卡片的
   删除逻辑上，变成误删源卡片。`paintSel()` 里仍保留一份同步（用于不重绘、只是切换选中的场景），
   两处要一起改，不能只改一处。
3. **单击选中的判定范围是 `.wrap`，不是整个 `.blk`。** `.blk` 几乎填满了正文列的宽度，
   若整块都拿来判定"点了就选中"，会把 `.gut`（版本号栏）、`.fold` 的空白、段落间的间隙
   全部占用，稿子会变得几乎无处可抓、拖不动位置。拖动整份稿子的排除表
   （`bindDoc` 里那个 `e.target.closest(...)`）跟这里的判定范围要保持对应：
   凡是这里判定"点了要选中"的范围，那边就必须排除在拖动之外，反之亦然。
4. **稿子范围内的双击必须整个拦住冒泡，不能只拦 `.cap`。** `stage` 自己有一套
   "双击空白画布新建卡片"的全局逻辑（`if(e.target.closest(".card"))return;addText(...)`），
   它完全不认识 `.doc`/`.blk`/`.cap` 这些写作页专用的元素。稿子是 `#stage` 的子孙节点，
   双击事件会一路冒泡上去，如果稿子内部不主动拦截，双击任何空白处（标题栏、版本号栏、
   折叠框……不只是双击段落本身）都会在画布上凭空新建一张卡片。修法是在 `bindDoc` 里
   给整个 `.doc` 挂一个 `dblclick` 监听只管 `stopPropagation()`，段落自己的双击进编辑
   逻辑再单独处理"该不该真的进入编辑"。这个坑第一次是靠跑真实浏览器的测试（不是静态走查）
   抓出来的，因为它只在真的派发一次原生双击手势时才会触发。

**新建的卡片/分身不上画布，见上面 `c.wrIn` 那一段。**

**行内样式要逐个属性赋值，不能拼字符串**：字体名里含引号，
拼进 `style="..."` 会把属性截断（症状是只剩 `font-family:`，字号全部失效）。
`wrStyleObj()` 返回对象，`wrApplyStyles()` 在渲染后 `Object.assign` 上去。

**「把这一版复制到剪贴板」要粘成画布上的普通卡片。** 这个菜单项的用途就是把稿子里的
某一版送回画布，所以 `verToClip()` 必须把 `wrIn`（只活在这份稿子里）连同 `wrUnder`、`h`
一起删掉——留着的话，粘出来的卡片会被 `syncCards()` 与 `docOrder()` 一并跳过：
卡片其实建出来了，可就是看不见、也不参与编号与导出，用起来就是"这个菜单没反应"。
剪贴板条目还要跟 `clipCards()` 生成的形状一致（`w` / `s` / `bg` 都得有），
否则 `pasteClip()` 会拿 `undefined` 去建卡片，粘出来的宽度和样式都是坏的。
`wrver` 组守着这一条。

**版本迭代**是这里的核心。`c.vers` 存版本数组，`c.verOn` 指向当前启用的那条，
而**当前内容始终留在 `c.text` / `c.rich`**。这个设计是刻意的：
其余所有功能（检索、导出、画布渲染、分身、表格）都不必知道版本的存在。
切换版本时 `verStash()` 先把当前内容写回它所属的版本槽，再把目标版本读进 `c.text`。
新增内容类字段时记得在 `verSnapshot` / `verStash` / `verApply` 三处一并处理，
否则那个字段不会随版本切换。版本连同 `s`（字体样式）和 `bg` 一起存，所以换写法可以换样子。
版本号用罗马数字（`ROMAN`），`verToClip()` 把某一版复制成独立卡片进剪贴板，
这是唯一会把版本外泄成新卡片的入口，且只在明确点菜单时发生。

侧栏目录由 `wrLevelOf()`（即卡片的 `level`）生成，与画布的分级标题是同一套，没有第二份层级数据。
`wrImport()` 按 `buildTree()` 的文档顺序而非点选顺序排列，所以送进来就是排好的稿子。

分身不再单独占一栏，而是通过 `c.wrUnder = 宿主卡片 id` 挂到某一段下方的折叠框里
（`wrKids(id)`）。判断分身用 `c.ref` 而不是 `orig(c).ref`：后者是源卡片，源本身没有 `ref`。

导出走 `openWrExport()`，四种格式：Word（`wrExportDocx`）、PDF（`wrHTML` + 打印对话框）、
HTML 与 Markdown（`wrMD`）。画面类导出对这种版式没有意义，所以不提供。
后两种不依赖任何外部库，断网也导得出来——Word 要现从 CDN 拉 docx 库，
只有它一条路的话，离线时整个导出就是死的。
两个开关控制是否附上其他版本与折叠的引用。

**导出面板必须调 `place()`。** `.pop` 是 `position:fixed` 但没有默认坐标，
不定位就会掉到文档流末尾去，表现为"导出框一团糟、点不动"。
类名也只能用 `.pop` 里真有的那几个（`.hd` `.sec` `.grid` `.opt` `.go` `.note`）；
早期这里自造了 `.ph` `.pb` `.hd2` `.fmt` `.fb2` 一套，全都没有对应样式。

**引文角色（`role:"quote"`）已在第 7 版删除**，迁移时退回普通卡片。
不要再加回来：文献归档由 `c.bib` 表达，写作中的引用由版本和分身表达，
再多一个角色只会让三套系统互相打架。

## 五、大纲：结构连线

`l.st` 标记一条连线为"结构连线"，表示下级关系。只要画布上存在结构连线，
`buildTree` 就改走 `outlineForest` + `buildFromForest`，编号和导出层级由树形关系决定，
不再依赖卡片的空间位置。可以用 `S.outline = false` 关掉，回到按位置推断。

**方向由标题层级决定，不看连线怎么画的**（`lvRank` / `structPair`）：
层级数字越小越靠上，没标层级的卡片 rank 记为 99，永远是末端。
只有两端 rank 相同时才回退到连线本身的 a→b 方向。
这样用户连线时不必在意方向，也不需要箭头。

判定为标题的条件：有结构子节点，或自己标了 `level`。纯末端且没标层级的算正文内容。
`lvRank` 决定谁是谁的上级，与连线画的方向无关：
标题按 level 取 1..n，`bib`（文献条目）95，正文 99，`quote`（原文记录）100。
**文献条目与它的原文之间不用连线**，因为那是归属关系而非画布走向。
原文卡片记 `c.bib = 文献条目 id`（注意：`c.src` 已被内联图片占用，不能复用这个名字），
`bindToBib()` 批量写入，`attachBibKids()` 在导出时把它们排到条目之后。
**归档与卡片角色是两套系统，不要互相赋值**：绑定只写 `c.bib`，不去改 `c.role`。
引文（`role:"quote"`）说的是这段文字在写作中的身份，文献条目说的是它归档在哪本文献名下，
一张卡片可以只占其一。
画布上只有文献条目本身带小圆点，归档在它名下的卡片不加标识。
点一下用透明度强调这一组（`bibFocus` / `paintBibFocus`），
圆点必须设 `z-index:8`，否则会被选中框盖住点不到。
`bibKids` 排除分身（`c.ref`），否则强调时会跳出一堆跨页面的重复卡片。
文献条目在画布上只加粗，**不做悬挂缩进**：缩进量带 `var(--iz)` 会随缩放变化导致文字重排，
看起来像变形。悬挂缩进只在导出的文档里用。
文献条目导出为悬挂缩进的加粗段落，原文导出为引用块
（Word 缩进斜体、HTML blockquote、Markdown 的 `>`）。
`bindToBib()` 是批量绑定：选中一条文献条目加若干卡片，一次建立结构连线。

**关联连线的语义是"排在一起"**（`pullTogether`），不是"另见"。
导出时把相连的非标题内容拉到相邻位置，只拉一次，不重复输出，标题不动。
可用 `S.relAdjacent = false` 关掉。两类连线都不再产生交叉引用文本：
结构连线体现为层级，关联连线体现为相邻。
没被结构连线串起来的卡片，按空间顺序接在文档后面，不会丢失。

**视觉区分只在混用时启用**：`hasStruct` 为真时，关联连线才画成虚线并略淡。
否则全部按用户自己设定的样式绘制。不这样做的话，打开旧文件会发现所有连线突然变虚线。

**导出里不再有任何"另见"式的指路文字**：结构连线体现为层级，关联连线体现为相邻，
分身在每一处完整呈现。原先的"生成交叉引用""附录关系表""分身写成交叉引用"三个导出选项，
以及 `relationsFor` / `relName` / `RELMAP` 都已随之删除，不要再加回来。

---

## 六、渲染与性能

**只渲染视野内的卡片。** `syncCards()` 按视口加边距（`CULL.m`，默认 700）决定
哪些卡片进入 DOM。用 `nodes.get(id)` 取卡片节点，**不要用 querySelector**，
因为视野外的节点根本不存在。另有 `NODE_CAP`（900）限制同时存在的节点数，
缩得很远时只渲染靠近视野中心的部分。

**剔除必须是节流，绝不能是防抖。** `cullSoon()` 里那个 `if(cullT)return`
是这一节最要紧的一行：它保证"最多每 90ms 跑一次，但连续输入期间一定会跑"。
早先每收到一次相机变化就 `clearTimeout` 把定时器往后推，于是一整个连续的
缩放或平移手势期间它一次都不跑，手势停下来才突然补齐。
实测代价：600 张卡片时，一次 40 步的缩放手势要 804ms（平均 20ms/步、峰值 68ms），
过程中 DOM 里一直挂着全部 600 个节点，结束后才骤降到 63。
症状就是用户说的"卡卡的，而且缩放之后内容渲染不出来"。改成节流后同样的手势降到 2ms。

**每帧的代价与节点数成正比，罪魁是 `--iz`。** 它挂在 `#world` 上，
是个会继承的自定义属性，一改就让整棵子树重算样式。实测：
600 个节点时单次 37.9ms，120 个节点时 6.4ms，而单纯改 `transform` 只要 0.03ms。
所以"先把节点剔干净"本身就是最有效的优化，比任何微调都管用。

**相机路径上不要做强制重排。** `camTo(...,true)` 早先同步调 `drawLinks()`（约 3ms），
`applyCam()` 每帧同步调 `syncBar()`（要读 `getBoundingClientRect`）。
写 transform 和读布局交替进行会让浏览器一帧内反复重算。
现在两者都合并到下一帧：`drawLinksSoon()` 与 `barSoon()`。

**样式改动不要整体重绘。** `setStyle()` 直接改已有节点的样式属性（`applyCardStyle`），
不调 `render()`。因为 `render()` 会销毁 contenteditable 元素，正在编辑的文字选区随之消失，
表现为"改字号没反应、数值跳回原值"。这是踩过的最深的坑之一。

### 性能实测

生成数据在真实浏览器里测得，拖动为实际鼠标事件下的帧率：

| 卡片数 | 数据量 | 打开 | 检索 | 编号 | 适应画面 | 拖动 | 平移 |
|---|---|---|---|---|---|---|---|
| 2000 | 0.2MB | 64ms | 20ms | 5ms | 6ms | 57fps | 16ms |
| 20000 | 2.4MB | 276ms | 80ms | 92ms | 65ms | 54fps | 17ms |
| 50000 | 6.0MB | 445ms | 98ms | 31ms | 88ms | 46fps | 22ms |
| 100000 | 12.1MB | 819ms | 142ms | 100ms | 136ms | 34fps | 30ms |
| 200000 | 24.5MB | 2096ms | 408ms | 328ms | 299ms | 17fps | 53ms |

**贴近真实用法的实测**（按章分页、短连线、大量引文与文献条目、标签、自动编号、
三成卡片锁定、二十张标准页）：

| 卡片 | 数据量 | 连线 | 页面 | 打开 | 编号 | 检索 | 地图 | 文献强调 | 锁定50张 | 拖动 | 平移 | 松手后 | 内存 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 3000 | 0.5MB | 2000 | 50 | 102ms | 17ms | 28ms | 13ms | 0ms | 5ms | 59fps | 17ms | 33ms | 11MB |
| 10000 | 1.8MB | 6666 | 167 | 229ms | 60ms | 42ms | 26ms | 1ms | 18ms | 59fps | 19ms | 39ms | 28MB |
| 30000 | 5.4MB | 20000 | 500 | 465ms | 212ms | 73ms | 31ms | 1ms | 44ms | 53fps | 28ms | 116ms | 61MB |
| 60000 | 10.9MB | 40000 | 1000 | 841ms | 389ms | 161ms | 47ms | 2ms | 136ms | 53fps | 44ms | 251ms | 117MB |

新功能都不构成负担：文献强调 1 到 2 毫秒，地图不到 50 毫秒，锁定五十张一百多毫秒，
标准页与页面框对拖动没有可测量的影响。

**极端配置**（长连线、全量结构连线）的实测：

| 卡片数 | 数据量 | 打开 | 编号 | 检索 | 适应画面 | 地图 | 拖动 | 松手后 | 内存 |
|---|---|---|---|---|---|---|---|---|---|
| 5000 | 0.6MB | 108ms | 21ms | 27ms | 8ms | 15ms | 63fps | 66ms | 11MB |
| 20000 | 2.4MB | 257ms | 120ms | 131ms | 21ms | 33ms | 56fps | 215ms | 30MB |
| 50000 | 6.1MB | 652ms | 278ms | 172ms | 63ms | 48ms | 58fps | 460ms | 61MB |
| 100000 | 12.2MB | 1322ms | 375ms | 238ms | 91ms | 70ms | 61fps | 908ms | 125MB |

十万张仍然可用，二十万张开始明显吃力。达到这个水平靠六处优化，**改动时不要退回去**：

1. **卡片索引** `cardIndex()`：id 到对象的 Map。失效判断同时比对数组引用和长度，
   因为导入、撤销、测试会整体替换 `S.cards`。只比长度会让索引指向已丢弃的旧对象，
   症状是"卡片存在但读出来是 undefined"。任何整体替换 `S.cards` 的地方都要
   先调 `invalidateIndex()`，且必须在 `render()` 之前。
2. **编号缓存** `numDirty`：只在 `render()` 时标脏。优化前五万张时每次 5066ms。
3. **分列与分带线性化**：`columns` / `band` 原本对每张卡片回头遍历已有的列，是平方级。
   排序后只与当前列比较即可。这是十万级最大的瓶颈，单这一项把 `docOrder`
   从 4924ms 降到 31ms。
4. **页面归属一次分桶** `bucketByFrame`：原本"每张卡片乘以每个页面"，同样是平方级。
5. **`frameOf` 二分查找**：页面按左边界排序后二分。
6. **连线不每帧清理**：断头连线的过滤是 O(连线数)，改为 `linksDirty` 标记。
   另有 `linkGrid` 空间网格，连线超过 400 条时启用。
7. **虚线有长度上限**（`DASH_MAX_SCREEN`，按屏幕像素判断）：虚线的绘制代价与路径长度成正比，
   浏览器要为每条虚线逐段计算分段。曾经几十条跨越整张画布的长虚线
   （路径总长 490 万像素）让单帧涨到 **3 秒**，去掉虚线属性立刻回到 32ms。
   超过阈值的连线退化为实线加更浅的颜色，视觉上仍能区分。
   阈值按"在屏幕上有多长"判断而不是世界坐标距离，因为缩小画布后长连线本来就不贵。
   **虚实只由连线身份决定**（结构实线、关联虚线），没有手动开关：
   手动的实线/虚线选项曾与这套自动规则打架，已删除，`l.dash` 字段在导入时一并丢弃。
   这是排查了快照、编号、连线清理、页面框、纸面、标签之后才定位到的真凶，
   `tests.js` 的 perf 组守着它，不要改回去。
8. **拖动只更新被牵动的连线**（`drawLinksLive`）：整块重建 SVG 的 innerHTML 会让浏览器
   重新解析并光栅化全部路径。拖动时改为只 `setAttribute("d")`，
   箭头与备注标记先隐藏、松手后随整体重绘复原（`endLinksLive`）。
9. **拖动的撤销快照延后**（`snapLazy` / `snapCommitIfMoved`）：`packState()` 在两万卡片时
   要 18ms、2.4MB，而大多数按下其实什么都没改。改为按下时只记录选中卡片的原位置，
   真的移动了才据此还原出移动前的状态入栈。
10. **拖动结束不重建整页**：位置变化可能影响编号，但整页重建要几百毫秒，
    改为 `refreshNums()` 只重算编号并就地更新徽标文字。
11. **松手后的重活都放到空闲时做**：重算编号（六万卡片时 500ms）走 `refreshNumsSoon`，
    撤销快照（200ms+）走 `requestIdleCallback`。松手后立刻可以继续操作，
    编号晚一瞬出现不影响使用。快照记录的是移动前的位置，延后执行不影响撤销的正确性。
12. **只改一个属性的操作不要 `render()`**：`setCardLock` 原本重建整页，
    三万卡片时要一秒以上，改为就地切换类名后降到几十毫秒。
    新增这类操作时先想清楚能否就地更新，`tests.js` 的 interact 组守着响应时间。

**性能排查的方法**：先用 `performance.now()` 量脚本耗时，若脚本很快而实际帧率很低，
说明瓶颈在浏览器绘制，此时要用 `requestAnimationFrame` 量真实帧间隔，
再逐个属性做对照实验（去掉虚线 / 去掉加粗 / 改颜色）才能定位。
另外测性能务必每档用全新的浏览器实例，同一实例连续跑多档会因未回收而读数失真。

**页面地图** 有自己的相机 `mapCam`（`{cx,cy,z}`，为 `null` 表示自动全览）。
三百个页面时全览下每页只有约 14 像素宽，所以支持滚轮缩放、拖动平移、双击全览，
面板尺寸存在 `S.mapW/mapH`。缩放下限略低于全览比例，缩回去时自动交还 `null`，
避免出现"缩不回整体视图"的状态。放大后按可见范围裁剪，四百页面两万卡片下绘制约 17ms。
用 canvas 绘制，不生成 DOM 节点；相机移动时通过 `mapSoon()` 节流刷新（90ms），
不要改成每帧直接调 `drawMap`。

---

**关闭右键菜单要挂在全局，不能只挂 `#stage`。** 专注模式下 `#stage` 是
`display:none`，只挂在它上面的话，在写作页内部点空白根本关不掉菜单，
非得点到页面外才行。现在是捕获阶段的全局 `pointerdown`：
不在任何一层菜单里就 `closeMenus()`。

---

## 七、工具栏的两条铁律

浮动文字工具栏（`buildBar`）有两条不能违反的规则：

**一、工具栏任何位置都不能夺取焦点。** 在捕获阶段统一 `preventDefault()`
（滑块除外，否则拖不动），这样点分隔线、标签、空白处都不会清掉文字选区。
注意捕获阶段**不能** `stopPropagation()`，否则控件自身的处理函数收不到事件。

**二、字号框不是输入框。** 它是只读显示区，点击后由程序接管键盘
（`beginSizeEdit` / `sizeKey`）。因为只要输入框获得焦点，文字选区就必然失焦，
这是浏览器的固有行为，靠事后恢复永远不稳。

局部格式的写入一律走 `document.execCommand`，目的是进入浏览器的编辑历史，
让 Ctrl+Z 能逐步回退。需要包一层样式时用 `wrapSelWith()`，它会补回被
`cloneContents()` 丢掉的祖先元素（浏览器不会克隆被部分选中的祖先，
直接用会把已有的颜色和加粗一起抹掉）。

有单元格选区时，对齐、加粗、斜体、文字色、底色都改为作用于选中的格子
（`applyCellFmt`），没有选区才作用于整张卡片。

工具栏定位：优先贴卡片上沿，放不下改贴下沿，两边都放不下才压在卡片上并自动半透明。
不要改成"钉在视口顶部"，那样会盖住表头。

---

## 八、加一个新功能要碰哪些地方

这是本项目最大的维护成本来源。**加任何带数据的功能，以下七处逐一检查：**

1. **渲染** `makeCard()` 或对应的绘制函数。内容类字段记得走 `orig()`。
2. **菜单** `cardItems` / `boardMenu` / `viewItems` / `fileItems` / `frameMenu` / `tableMenu`。
3. **文案** `T.en` 和 `T.zh` **同时**加，测试会检查两边一一对应。
   历史上有三次把中文插进了英文块，每次都是测试挡下来的。
4. **导入规整** `normCard()`（卡片）或 `absorb()` 里的连线规整。
   新字段要做类型校验，否则坏数据会让渲染崩溃。
5. **导出** 四条路径都要考虑：`exportDocx`、`docHTML`（PDF 与 HTML 共用）、
   `docMD`（Markdown 与存档包）、`captureCanvas`（画面）。
6. **版本迁移** 若改了数据结构，`SCHEMA` 加一，在 `migrate()` 里补一段。
7. **测试** 在 `tests.js` 里加一组，哪怕只有两三条断言。

漏掉第 4 条会导致旧文件打不开，漏掉第 5 条会导致资料无法归档，这两样最严重。

还有一条不在"七处"里、但同样容易漏的：**凡是新开一条"把卡片送进稿子"的路径
（拖放、导入、批量转换……都算），顺序必须过 `wrOrderIds` / `wrOrderItems`。**
不过一次，出来的就是卡片的创建顺序，用户看到的是层级完全错乱。见第四点五节。

---

## 九、版本迁移

`SCHEMA` 是当前数据结构版本，`migrate(d)` 是迁移链，按 `d.v` 逐级往上补。

已有的迁移：
- 1 → 2：连线显示默认值修正
- 2 → 3：补齐页面与模板字段
- 3 → 4：取消编组，已锁定的组转为卡片自身锁定
- 4 → 5：锁定分两级，旧的布尔 `lock:true` 一律转为 `"all"`
- 5 → 6：文献条目的归属不再用连线表达，旧的结构连线转为 `c.bib` 字段
- 6 → 7：引文角色取消，`role:"quote"` 退回普通卡片
- 7 → 8：折叠框里的分身补上 `c.wrIn`，从此只活在所属的稿子里，不再上画布

原则：

- **只增不减。** 永远不要删除旧字段的读取逻辑，哪怕程序早就不用了。
- **容错优先于正确。** `normCard()` 遇到缺失或类型不对的字段一律修正为合理默认值，
  绝不抛错。导入失败比导入不完美严重得多。
- **比当前版本更新的文件也要尽力打开**，只在控制台留个警告。

---

## 十、数据安全的五层

这是这个工具最要紧的部分，因为它承载的是多年积累。

| 层 | 防什么 | 机制 |
|---|---|---|
| 锁定 | 误碰已定稿内容 | 卡片级两段锁，连带锁住相关连线 |
| 撤销 | 手滑 | 内存里 40 步 |
| 自动快照 | 误操作且已自动保存 | 每 10 分钟一份存进 IndexedDB，留最近 12 份加按天 7 份 |
| 绑定文件 | 浏览器数据被清 | File System Access API 自动写回本地文件 |
| 存档包 | 程序失效、平台变更、多年以后 | zip，内含分页 Markdown、图片原件、完整 json |

**存档包是最后的防线，它的价值在于不依赖本程序。**
`pages/` 是纯文本，`assets/` 是普通图片，用记事本和看图软件就能读，
也能直接倒进别的笔记软件。json 只是"能完美还原"的那一层，不是唯一出路。

zip 打包器（`zipMake`）是手写的六十行，无压缩、无依赖，离线可用。
不要为了省事换成外部库，那会重新引入网络依赖。

有两处保护是隐性的，改动时别弄丢：清空画布会自动解除文件绑定；
画布为空时暂停自动写回。两者都是防止"清空后自动保存把原文件覆盖成空的"。

建议的节奏：日常靠自动快照防误操作；每天结束靠绑定文件自动写回；
每完成一个阶段导一次存档包放进云盘；定期往移动硬盘归档一份存档包（不是工作文件），
按月建目录，只增不覆盖。

---

## 十一、已知的取舍与限制

**撤销快照 `packState()` 现在包含 `S.docs`。** 早期版本只打包了
`sheets/frames/links/cards`，写作页（`S.docs`）完全不在撤销范围内——
在写作页里插入、移出、改名、切模式，Ctrl+Z 一律无效，改动会悄悄留下。
`applyUndo()` 现在会一并还原 `S.docs`，并在专注模式开着时补一次 `drawWrite()`
刷新覆盖层。以后往 `S` 上加新的顶层数组/对象时，先想一下它要不要参与撤销。

**图片有孤儿。** 替换或删除图片后，旧图仍留在 IndexedDB 里。没有做自动清理，
因为图片是跨画布共享的，自动清理有误删其他文件所需图片的风险。

**画面导出依赖网络字体。** 中文已全部改用系统字体；英文字体来自 Google Fonts，
若未加载完就导出，可能截到回退字体。渲染库本身已内置，断网可用。

**`buildBar` 与 `makeCard` 偏长**，是目前最该拆的两处。若要重构，注意第七节的两条铁律。

**何时该拆文件**：自有脚本超过 250 KB，或某个功能需要同时改七处以上时。
拆成 ES 模块在静态托管上没问题，代价是不能再双击本地文件直接打开
（模块受同源限制），需要加一个把模块拼回单文件的构建步骤。
目前自有脚本 204 KB，已经接近这条线。

---

## 十二、改动这份代码的建议做法

不要用字符串批量替换。这份代码里出过多次事故都源于此：
把 `hOf` 的定义本身也替换了变成自我调用；把中文文案插进英文文案区（三次）；
抽取公共函数时漏改一处引用导致整个功能抛错。

如果非要批量替换，**排除定义本身**，并且改完立刻跑 `node tests.js`。
补丁脚本要逐条独立应用、失败不影响其他条目，否则一处断言失败会让整批改动静默丢失。

改完之后的检查顺序：

1. `node tests.js` 全绿
2. 手动开一次程序，随便点点，看控制台有没有报错
3. 导出一次存档包，解压看看内容对不对
4. 用旧版本的 json 文件导入一次，确认还能打开
